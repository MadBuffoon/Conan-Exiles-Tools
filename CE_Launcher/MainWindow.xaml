<Window x:Class="CE_Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CE_Launcher" Height="125" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <ComboBox x:Name="TxtFileDropdown" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="0,0,0,10"/>
        <Button x:Name="LaunchButton" Grid.Row="1" Content="Launch" Width="75" VerticalAlignment="Center" HorizontalAlignment="Right" Click="LaunchButton_Click"/>
        
        <!-- Checkbox for closing the window on launch -->
        <CheckBox x:Name="CloseOnLaunchCheckBox" Grid.Row="1" Content="Close On Launch" VerticalAlignment="Center" HorizontalAlignment="Left"/>
    </Grid>
</Window>